// @ts-nocheck
// Entry bridge — the real application lives in /frontend (see project structure).
// All frontend source code is organised inside the `frontend/` folder:
//   frontend/src/{components,pages,layouts,hooks,services,context,utils,routes}
export { default } from "../frontend/src/App";
